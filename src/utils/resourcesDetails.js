const generateResourceDetails = (name, userCapacity, isActive) => {
    return {
        username,
        text,
        createdAt: new Date().getTime()
    }

}

module.exports = generateResourceDetails
